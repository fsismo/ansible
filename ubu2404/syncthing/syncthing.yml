---
- hosts: alphaprime-dev.sismonda.local
  become: yes

  tasks:
  - name: Include Docker / Docker Compose Task
    ansible.builtin.include_tasks: "../modules/docker.yml"

  - name: Crear directorio /opt/docker/minecraft-java
    file:
      path: /opt/docker/syncthing
      state: directory

  - name: Copiar Compose
    template:
      src: /mnt/storage/ansible/ubu2404/syncthing/opt/docker/syncthing/compose.yml
      dest: /opt/docker/syncthing/compose.yml
      owner: root
      group: root
      mode: 0640
      backup: yes

  - name: Copiar do.sh
    template:
      src: /mnt/storage/ansible/ubu2404/syncthing/opt/docker/syncthing/do.sh
      dest: /opt/docker/syncthing/do.sh
      owner: root
      group: root
      mode: 0740
      backup: yes

  - name: Copiar syncthing.service
    template:
      src: /mnt/storage/ansible/ubu2404/syncthing/etc/systemd/system/syncthing.service
      dest: /etc/systemd/system/syncthing.service
      owner: root
      group: root
      mode: 0644
      backup: yes
      
  - name: Configurar ejecución de syncthing.service.
    systemd:
      name: syncthing.service
      state: started
      daemon_reload: yes
      enabled: yes
